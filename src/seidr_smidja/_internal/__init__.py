"""seidr_smidja._internal — Private infrastructure layer.

This package contains shared low-level infrastructure used internally
by multiple domains. It is NOT part of the public API.

External consumers of seidr_smidja must never import from _internal.
"""
