"""Horfunarþjónn endpoint modules.

Sub-modules:
    health      — GET /v1/brunhand/health (no auth)
    capabilities — GET /v1/brunhand/capabilities (auth required)
    primitives  — POST /v1/brunhand/* (9 generic GUI primitives)
    vroid       — POST /v1/brunhand/vroid/* (3 VRoid-specific scripts)
"""
